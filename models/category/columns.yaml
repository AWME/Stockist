# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: awme.stockist::lang.categories.name
        searchable: true
    slug:
        label: Slug
        searchable: false
    is_enabled:
        label: awme.stockist::lang.categories.is_enabled
        type: switch
    is_visible:
        label: awme.stockist::lang.categories.is_visible
        type: switch
